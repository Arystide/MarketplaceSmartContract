name: GitHub Actions Demo
on: [push]
jobs:
  Unit-Testing:
    runs-on: ubuntu-latest
    steps:
      - run: cd /home/runner/work/MarketplaceSmartContract & npm i
      - run: ls
      - run: npm i
      - run: npx hardhat test --show-stack-traces
 
